# 커버리지와 검증 기준

현재 기준: 22개 내장 예제, 기본·경계 62건 + 이름 변경 22건의 실행 시나리오, 21개 검출 패턴.
패턴은 겹칠 수 있습니다. 예를 들어 다익스트라는 최단경로와 힙 패턴을 함께 갖습니다.

## 패턴 → 근거 → 시각화

| 패턴 ID             | 코드에서 찾는 근거                             | 우선 시각화 / 연결 정보            | 대표 예제         |
| ------------------- | ---------------------------------------------- | ---------------------------------- | ----------------- |
| sorting             | 정렬 호출 또는 같은 배열의 두 위치 교환        | 고정 눈금의 부호 있는 막대         | 버블, 삽입        |
| binary_search       | 중간 인덱스 계산과 경계 갱신                   | 배열, 코드에서 찾은 경계·중간 변수 | 이진 탐색         |
| two_pointers        | while에서 두 경계로 같은 배열 접근             | 배열과 두 인덱스                   | 두 수의 합        |
| sliding_window      | 같은 누적값에 원소를 더하고 빼기               | 배열 접근 위치와 누적 변수         | 고정 길이 구간 합 |
| prefix_sum          | 이전 누적값 참조 또는 누적값 저장              | 누적값 테이블                      | 누적 합           |
| bfs                 | popleft와 인접 목록 연산                       | 방향 그래프와 큐                   | BFS               |
| dfs                 | 재귀 함수의 인접 목록 순회                     | 그래프와 호출 스택                 | DFS               |
| grid_bfs            | FIFO 큐와 행·열 접근                           | 격자, 큐·거리 상태                 | 격자 최단거리     |
| dijkstra            | 힙 연산과 비교 후 비용 갱신                    | 가중 그래프, 힙·거리 배열          | 다익스트라        |
| topological         | 차수 감소, 0 검사, 큐 삽입                     | 그래프와 진입 차수·큐              | 위상 정렬         |
| union_find          | 부모를 따라 재귀 또는 부모의 부모 참조         | 자식→부모 연결, 대표 정점          | 집합 합치기       |
| dynamic_programming | 같은 테이블의 여러 이전 상태 또는 min/max 참조 | 상태 테이블의 읽기/쓰기            | 피보나치, 배낭    |
| backtracking        | 같은 재귀 함수의 append/pop                    | 선택 스택과 함수 지역 변수         | 조합              |
| recursion           | 자기 자신을 호출                               | 호출 스택과 반환 값                | 팩토리얼          |
| heap                | heapq 연산과 대상 컨테이너                     | 이진 힙의 부모/자식 관계           | 최소 힙           |
| queue               | popleft                                        | FRONT/BACK                         | BFS               |
| stack               | 같은 범위의 append와 인자 없는 pop             | 세로 스택/TOP                      | 괄호 검사         |
| frequency           | Counter 또는 키별 get 갱신                     | 키/값과 빈도 막대                  | 빈도 집계         |
| string_matching     | 이전 일치 길이로 되돌아가는 실패 함수 참조     | 실패 함수 테이블, 문자열           | KMP               |
| bitmask             | 비트 AND/OR/XOR/이동                           | 비트 위치와 켜진 상태              | 부분집합          |
| grid                | 중첩 인덱스 접근                               | 행·열 셀                           | 2차원 순회        |

이는 인식 규칙의 범위입니다. 모든 구현 변형, 알고리즘의 정당성, 문제 제약을 판정하는 목록은 아닙니다.
같은 코드가 복수 패턴에 해당하면 근거를 모두 제공하고 우선순위에 따라 대상을 추천합니다.
추천이 맞지 않으면 변수/표시 방식을 직접 고를 수 있습니다.

## 샘플 입력의 선택 기준

| 입력 종류      | 기본 흐름                    | 경계·분기                           |
| -------------- | ---------------------------- | ----------------------------------- |
| 정렬           | 위치 이동이 발생하는 수열    | 이미 정렬됨, 중복·음수              |
| 이진 탐색      | 원하는 값이 존재             | 값 없음, 원소 하나                  |
| 그래프 탐색    | 여러 경로와 방문 중복 가능성 | 정점 하나, 연결되지 않은 정점       |
| 최단경로       | 더 나은 비용으로 갱신        | 시작점만 존재, 도달 불가            |
| 위상 정렬      | 선행 관계가 있는 DAG         | 간선 없음, 사이클                   |
| 집합           | 여러 집합을 연결             | 합치기 없음, 중복 합치기            |
| DP·구간·문자열 | 계산/선택/일치가 발생        | 예제별 최소 크기·해 없음·반복 값 등 |

내장 샘플은 출력까지 실행 검증합니다. 사용자 코드 제안은 기본/작은 크기의 정적 후보입니다.
입력 순서·반복 횟수·행과 열을 맞추되, 코드만으로 알 수 없는 값의 의미나 제약을 정답처럼 제시하지 않습니다.
전체 토큰 읽기, 조건별 입력, 사용자 정의 파서 등은 생성 보류 사유를 표시합니다.

## 검증

- 추적 엔진, 분석기, 입력 생성기: Pyodide에서 Python 테스트 80개
- 내장 예제: 22개 / 기본·경계 62건 + 변수·함수 이름 변경 22건, 예상 출력과 필수 패턴 확인
- 시각화 모델: 음수 눈금·음수 인덱스·가중치·범위 초과·수동 보기 등 19개 검증 + 사용자 코드 보기 18건
- TypeScript 타입 검사, 정적 빌드와 하위 경로 자산 확인
- 브라우저 조작·반응형 시각 QA는 아직 수행하지 않음

## 현재 한계와 다음 확장 방향

1. **알고리즘별 전용 구조 확장**: 세그먼트 트리/Fenwick, MST, 트라이, 문자열 해시, 최단경로의 다른 방식, 기하 알고리즘은 전용 인식과 배치를 아직 제공하지 않습니다. 지원되는 Python 코드로 실행하면 일반 값·컨테이너와 추적 기록은 볼 수 있습니다.
2. **데이터 흐름 분석**: 기본 컨테이너의 동일 객체 관계로 함수 인자·반환·별칭을 연결합니다. 사용자 클래스, 컨테이너를 변환·복사하는 함수의 의미, 라이브러리 내부 동작은 후속 분석이 필요합니다.
3. **입력 스키마 보완**: 문제의 제약이나 사용자가 지정한 입력 형식을 함께 받으면 0/1 기반 정점, 문자열 문자 집합, 유효 범위 등을 더 정확히 구성할 수 있습니다.
4. **접근 분석 정교화**: 현재 위치 표시는 줄 실행 전의 값을 사용합니다. 단락 평가와 연쇄 비교의 건너뛴 접근은 제외합니다. 부작용이 있는 호출 뒤의 접근과 comprehension 내부 반복은 예측을 생략하며, 전용 추적이 후속으로 필요합니다.
5. **큰 구조와 동시 보기**: 관련 자료구조는 요약 카드로 표시하고, 이웃 인덱스와 연결된 상태 배열은 그래프 정점 아래에도 표시합니다. 큰 그래프 탐색, 계층형 트리 배치, 여러 구조의 동기화된 전용 캔버스를 후속으로 확장할 수 있습니다.

커버리지를 늘릴 때에는 **인식 규칙 + 설명 근거 + 시각화 + 기본/경계/실패 테스트**를 같이 추가합니다.
예제 이름을 추가한 것만으로 해당 유형 전체를 지원한다고 표시하지 않습니다.
